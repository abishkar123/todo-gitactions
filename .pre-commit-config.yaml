repos:
  - repo: local
    hooks:
      - id: staged-secret-scan
        name: staged secret scan
        entry: .githooks/pre-commit
        language: system
        pass_filenames: false
        stages: [pre-commit]
